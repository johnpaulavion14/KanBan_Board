<%= form_with(model: addcard) do |form| %>
  <% if addcard.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(addcard.errors.count, "error") %> prohibited this addcard from being saved:</h2>

      <ul>
        <% addcard.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :card_name, style: "display: block" %>
    <%= form.text_field :card_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
